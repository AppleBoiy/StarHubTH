"""Shared constants for release.py.

Kept separate from release.py so the app name and bundle name can't silently
drift if another script ever needs them too.
"""

APP_NAME = "StarHubTH"
APP_DIR = f"{APP_NAME}.app"
